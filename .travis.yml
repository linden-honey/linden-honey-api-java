sudo: required
dist: trusty
language: java
jdk:
  - oraclejdk8
services:
  - postgresql
env:
  - LINDEN_HONEY_DB_NAME=linden_honey_local linden_honey_db_user_login=postgres
before_script:
  - psql -c 'create database linden_honey_local;' -U postgres

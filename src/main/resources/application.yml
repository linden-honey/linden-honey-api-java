# ===============================
# [ TG2VK Properties ]
# ===============================
linden_honey:
  app:
    name: Linden Honey
  db:
    url: jdbc:postgresql://localhost:5432/linden-honey?user=linden-honey&password=linden-honey


# ===============================
# [ Spring Properties ]
# ===============================
spring:
  application:
    name: ${linden_honey.app.name}
  datasource:
    url: ${linden_honey.db.url}
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        id:
          new_generator_mappings: true


# ===============================
# [ Flyway Properties ]
# ===============================
flyway:
  enabled: true
  locations: classpath:db/migration


# ===============================
# [ Logging Properties ]
# ===============================
logging:
  level:
    com:
      github:
        lindenhoney: DEBUG
    org:
      springframework: INFO
      hibernate: ERROR
      flywaydb: INFO
